program := {statement}

statement := function | variable | struct

struct := "struct" identifier "{" {variable} "}"

variable := identifier

function := "func", ident, "(", {variable, ","} ")" {variable}

identifier := letter {letter}
